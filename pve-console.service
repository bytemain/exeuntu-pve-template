[Unit]
Description=Exeuntu console autologin (exedev)
Documentation=https://github.com/bytemain/exeuntu-pve-template
After=systemd-user-sessions.service getty-pre.target
Conflicts=rescue.service
Before=getty.target
ConditionPathExists=/dev/console

[Service]
# Open the PVE console directly into the non-root exedev user with no key or
# password. The outer surface (PVE login / Raft login) is the access boundary.
# agetty opens /dev/console directly (no stdin "-" and no serial baud rates,
# which would make systemd hand it a pty instead of the console device).
ExecStart=-/sbin/agetty --autologin exedev --noclear /dev/console linux
Type=idle
Restart=always
RestartSec=0
TTYReset=yes
TTYVHangup=yes
UtmpIdentifier=console
IgnoreSIGPIPE=no
SendSIGHUP=yes

[Install]
WantedBy=multi-user.target
